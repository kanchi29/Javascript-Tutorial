// DOM tree refers to the HTML page where all the nodes are objects.
// 3 main types of noides: Text nodes, Element nodes, Comment nodes
//<html> is at the root, <head> and <body> are its children 
//text node is always the leaf node of the tree


//Autocorrection is when some erroneous html is encountered by the browser it tries to avoid or correct tat error
// Ex- if something is written is written outside a body tag browser will addit inside the body tag. 